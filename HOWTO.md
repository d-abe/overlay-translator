# Overlay Translator - 使い方ガイド

## 概要

Overlay Translatorは、画面上の任意の領域を選択して、その中のテキストを自動的にOCR（文字認識）し、AIで日本語に翻訳してオーバーレイ表示するWindowsデスクトップアプリケーションです。

## 主な機能

- 📸 **画面キャプチャ**: ホットキーで矩形選択した領域を自動キャプチャ
- 🔍 **OCR（文字認識）**: Google Cloud Vision API、Groq Vision API、またはEasyOCRを使用
- 🌐 **AI翻訳**: Groq APIを使用してテキストを日本語に翻訳
- 🎨 **オーバーレイ表示**: 選択範囲に翻訳結果を背景色に合わせて表示
- ⚙️ **設定画面**: GUIでAPIキーやホットキーを簡単に設定
- 📝 **ログ出力**: デバッグ情報を`translator.log`に出力

## 初回セットアップ

### 1. アプリケーションの起動

#### EXEファイルを使用する場合
1. `OverlayTranslator.exe`をダブルクリックして起動
2. タスクトレイ（通知領域）にアイコンが表示されます

#### Pythonで実行する場合
```bash
python main.py
```

### 2. APIキーの設定

アプリケーションを起動後、まずAPIキーを設定する必要があります。

#### 方法1: 設定画面から設定（推奨）

1. タスクトレイアイコンを**右クリック**
2. **「設定」**を選択
3. 設定画面が開きます

**必須設定:**
- **Groq APIキー**: 翻訳機能に使用
  - 取得方法: https://console.groq.com/ でアカウントを作成し、APIキーを取得

**OCR方法の選択:**
以下のいずれかを選択してください：

- **Google Cloud Vision API**（高精度・請求が必要）
  - `GOOGLE_API_KEY`を設定
  - または`GOOGLE_APPLICATION_CREDENTIALS`でサービスアカウントJSONファイルのパスを指定

- **Groq Vision API**（高速・Groq APIキー使用）
  - Groq APIキーが設定されていれば使用可能
  - 追加の設定は不要

- **EasyOCR**（オフライン・初回のみモデルダウンロード）
  - インターネット接続不要（初回ダウンロード後）
  - 初回実行時に約500MBのモデルファイルをダウンロード

4. 各項目を入力後、**「保存」**をクリック

#### 方法2: .envファイルで直接設定

1. `.env.example`ファイルをコピーして`.env`ファイルを作成
2. `.env`ファイルをテキストエディタで開く
3. 以下のようにAPIキーを設定：

```env
# 翻訳用APIキー（必須）
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-8b-instant

# OCR方法の選択（google, groq, easyocr のいずれか）
OCR_METHOD=google

# Google Cloud Vision API（OCR_METHOD=googleの場合）
GOOGLE_API_KEY=your_google_api_key_here
# またはサービスアカウントJSONファイルのパス
# GOOGLE_APPLICATION_CREDENTIALS=path/to/key.json

# ホットキー設定（オプション、デフォルト: ctrl+shift+t）
HOTKEY=ctrl+shift+t

# オーバーレイ表示設定（オプション）
OVERLAY_FONT_FAMILY=Meiryo
OVERLAY_FONT_SIZE=12
OVERLAY_FONT_STYLE=normal

# デバッグログ（オプション、デフォルト: false）
DEBUG_LOGGING=false
```

### 3. ホットキーの設定

デフォルトのホットキーは`Ctrl+Shift+T`です。

**ホットキーを変更する場合:**
1. 設定画面を開く
2. 「ホットキー設定」セクションでホットキーを入力
   - 例: `ctrl+shift+t`, `alt+f1`, `ctrl+alt+s`
   - 複数のキーは「+」で区切ります
3. 「保存」をクリック（変更は即座に反映されます）

## 基本的な使い方

### 1. 矩形選択モードの開始

1. 設定したホットキー（デフォルト: `Ctrl+Shift+T`）を押す
2. 画面全体が半透明のオーバーレイで覆われます
3. マウスでドラッグして翻訳したい領域を選択
4. マウスボタンを離すと自動的に処理が開始されます

**キャンセルする場合:**
- `ESC`キーを押すと矩形選択モードをキャンセルできます

### 2. 翻訳結果の表示

1. 選択範囲が自動的にキャプチャされます
2. OCR処理でテキストが抽出されます
3. AI翻訳で日本語に翻訳されます
4. 翻訳結果が選択範囲にオーバーレイ表示されます

### 3. オーバーレイの操作

- **閉じる**: 右上の「×」ボタンをクリック、またはオーバーレイ内をクリック
- **リサイズ**: 右下のリサイズハンドルをドラッグしてサイズを調整可能
- **フォント変更**: 設定画面の「オーバーレイ表示設定」からフォントを変更可能

## 設定画面の詳細

### Groq API設定（翻訳用）

- **APIキー**: Groq APIキーを入力（必須）
- **モデル**: 使用するGroqモデルを選択
  - 推奨: `llama-3.1-8b-instant`（高速・低コスト）
  - 高品質: `llama-3.3-70b-versatile`, `openai/gpt-oss-120b`
  - 最新のモデルリスト: https://console.groq.com/docs/models

### Google Cloud Vision API設定（OCR用）

- **APIキー**: Google Cloud Vision APIキーを入力
- **サービスアカウント JSONファイル**: サービスアカウントJSONファイルのパスを指定（「参照」ボタンで選択可能）

### OCR方法

3つの方法から選択できます：

1. **Google Cloud Vision API**
   - 高精度なOCR
   - インターネット接続が必要
   - Google Cloudの請求が必要
   - EasyOCRモデルは不要

2. **Groq Vision API**
   - 高速なOCR
   - Groq APIキーを使用
   - インターネット接続が必要
   - EasyOCRモデルは不要

3. **EasyOCR**
   - オフラインで動作可能（初回ダウンロード後）
   - 初回実行時に約500MBのモデルファイルをダウンロード
   - インターネット接続不要（初回ダウンロード後）

### ホットキー設定

- 矩形選択を開始するホットキーを設定
- 例: `ctrl+shift+t`, `alt+f1`, `ctrl+alt+s`
- 複数のキーは「+」で区切ります

### オーバーレイ表示設定

- **フォント**: フォントファミリー、スタイル（Regular/Bold/Italic）、サイズを選択
- 「フォントを選択...」ボタンから設定画面を開けます

### デバッグ設定

- **DEBUGログを有効にする**: チェックを入れると、詳細なデバッグ情報が`translator.log`に出力されます

## トラブルシューティング

### アプリケーションが起動しない

1. コマンドプロンプトから実行してエラーメッセージを確認
2. `.env`ファイルが正しく設定されているか確認
3. `translator.log`ファイルを確認（エラー情報が記録されています）

### OCRが動作しない

1. **Google Cloud Vision APIを使用する場合:**
   - APIキーが正しく設定されているか確認
   - Google Cloud Consoleで請求が有効になっているか確認
   - インターネット接続を確認

2. **Groq Vision APIを使用する場合:**
   - Groq APIキーが正しく設定されているか確認
   - インターネット接続を確認

3. **EasyOCRを使用する場合:**
   - 初回実行時にモデルファイルがダウンロードされるまで待つ
   - インターネット接続を確認（初回ダウンロード時）

### 翻訳が動作しない

1. Groq APIキーが正しく設定されているか確認
2. インターネット接続を確認
3. `translator.log`ファイルでエラーを確認

### ホットキーが動作しない

1. 設定画面でホットキーが正しく設定されているか確認
2. 他のアプリケーションが同じホットキーを使用していないか確認
3. ホットキーの形式が正しいか確認（例: `ctrl+shift+t`）

### オーバーレイが表示されない

1. 画面の端に表示されている可能性があります（位置を確認）
2. 他のウィンドウの下に隠れている可能性があります
3. `translator.log`ファイルでエラーを確認

## ログファイル

アプリケーションの実行ログは`translator.log`に出力されます。

- **場所**: EXEファイルと同じディレクトリ、またはプロジェクトルート
- **内容**: エラー情報、デバッグ情報（DEBUG_LOGGINGが有効な場合）、OCR結果など
- **確認方法**: テキストエディタで開いて確認

## よくある質問

### Q: Pythonのインストールは必要ですか？

**A:** EXEファイルを使用する場合、Pythonのインストールは不要です。すべての依存関係がEXEファイルに含まれています。

### Q: インターネット接続は必要ですか？

**A:** 
- **Google Cloud Vision API / Groq Vision APIを使用する場合**: インターネット接続が必要です
- **EasyOCRを使用する場合**: 初回のみモデルファイルのダウンロードにインターネット接続が必要です。その後はオフラインで動作可能です

### Q: どのOCR方法を選べばよいですか？

**A:**
- **高精度が必要**: Google Cloud Vision API
- **高速性を重視**: Groq Vision API
- **オフラインで使用したい**: EasyOCR

### Q: ホットキーを変更できますか？

**A:** はい、設定画面からいつでも変更可能です。変更は即座に反映されます。

### Q: フォントを変更できますか？

**A:** はい、設定画面の「オーバーレイ表示設定」からフォントファミリー、スタイル、サイズを変更できます。

### Q: ログファイルは削除しても大丈夫ですか？

**A:** はい、削除しても問題ありません。次回起動時に自動的に再生成されます。

## サポート

問題が解決しない場合は、`translator.log`ファイルの内容を確認してください。エラーメッセージが記録されています。

